
let Test = {
	init(APP) {
		// return;

		// setTimeout(() => APP.els.content.addClass("show-pre-game"), 100);

		// setTimeout(() => APP.dispatch({ type: "replay-race" }), 100);
		// setTimeout(() => karaqu.shell("win -a"), 300);

		// setTimeout(() => APP.dispatch({ type: "replay-race", mode: "replay" }), 300);
		// setTimeout(() => APP.els.content.find(".view-game").addClass("show-pause"), 1000);
	}
};
